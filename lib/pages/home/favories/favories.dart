import 'package:flutter/material.dart';

class Favories extends StatelessWidget {
  const Favories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Favories')),
    );
  }
}
